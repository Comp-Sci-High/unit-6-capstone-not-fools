<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Cardverse - TCG Marketplace</title>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <header>
  <h1>CardCanopy</h1>
  <nav>
    <a href="/">Home</a>
    <a href="/pokemon">Pokémon</a>
    <a href="/magic">Magic: The Gathering</a>
    <a href="/yugioh">Yu-Gi-Oh!</a>
  </nav>

  <!-- Cart Icon and Cart Count -->
  <div id="cart">
    <button onclick="toggleCart()">
      <img src="https://images.vexels.com/media/users/3/155845/isolated/preview/8840ae67a4baa51b2ce899a7fbb783f7-push-cart-icon-graphic.png" alt="Icon" width="16" height="16">
      Cart (<span id="cart-count">0</span>)
    </button>

    <div id="cart-items" class="cart-items">
  <h3>Your Cart</h3>
  <ul id="cart-list"></ul>
  <button id="clear-cart">Clear Cart</button>
  <button id="checkout-btn" onclick="location.href='checkout.html'">Checkout</button>
</div>
  </div>
</header>

  <main>
  <section id="pokemon">
    <h2>Pokémon Cards</h2>
    <div class="card-grid">
      <% for(let x = 0; x < cards.length; x++) {%>
        <div class="card" data-card="<%= cards[x].id %>">
          <img src="<%= cards[x].image %>" alt="<%= card.name %>">
          <h3><%= card.name %></h3>
          <p>$<%= card.price.toFixed(2) %></p>
          <button onclick="addToCart('<%= cards[x].name %>', `<%= cards[x].price %>`)">Add to Cart</button>
        </div>
      <% } %>
    </div>
  </section>

  <section id="magic">
    <h2>Magic: The Gathering Cards</h2>
    <div class="card-grid">
      <% for(let x = 0; x < cards.length; x++) {%>
        <div class="card" data-card="<%= card[x].id %>">
          <img src="<%= cards[x].image %>" alt="<%= card.name %>">
          <h3><%= card.name %></h3>
          <p>$<%= card.price.toFixed(2) %></p>
          <button onclick="addToCart('<%= cards[x].name %>', `<%= cards[x].price %>`)">Add to Cart</button>
        </div>
      <% } %>
    </div>
  </section>

  <section id="yugioh">
    <h2>Yu-Gi-Oh! Cards</h2>
    <div class="card-grid">
      <% for(let x = 0; x < cards.length; x++) {%>
        <div class="card" data-card="<%= cards[x].id %>">
          <img src="<%= cards[x].image %>" alt="<%= card.name %>">
          <h3><%= card.name %></h3>
          <p>$<%= card.price.toFixed(2) %></p>
          <button onclick="addToCart('<%= cards[x].name %>', `<%= cards[x].price %>`)">Add to Cart</button>
        </div>
      <% } %>
    </div>
  </section>
</main>
<footer>
    <a href="https://www.instagram.com/cardcanopy/" target="_blank" style="color: rgb(255, 74, 252); text-decoration: none; display: inline-flex; align-items: center; gap: 5px;">
      <img src="https://upload.wikimedia.org/wikipedia/commons/a/a5/Instagram_icon.png" alt="Instagram" width="16" height="16">
      &copy; Follow me on Instagram for updates and more! (click me)
    </a>
  </footer>

  <script src="cart.js"></script>
</body>
</html>
